import { SplitInterfacePipe } from './split-interface.pipe';

describe('SplitInterfacePipe', () => {
  it('create an instance', () => {
    const pipe = new SplitInterfacePipe();
    expect(pipe).toBeTruthy();
  });
});
