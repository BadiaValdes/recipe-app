import { InputHostInUseDirective } from './input-host-in-use.directive';

describe('InputHostInUseDirective', () => {
  it('should create an instance', () => {
    const directive = new InputHostInUseDirective();
    expect(directive).toBeTruthy();
  });
});
