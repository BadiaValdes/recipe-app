import { RecipeNameDirective } from './recipe-name.directive';

describe('RecipeNameDirective', () => {
  it('should create an instance', () => {
    const directive = new RecipeNameDirective();
    expect(directive).toBeTruthy();
  });
});
