import { PrincipalCheckDirective } from './principal-check.directive';

describe('PrincipalCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new PrincipalCheckDirective();
    expect(directive).toBeTruthy();
  });
});
